# Runtime dependencies for the deployed demo app (demo/app.py) only.
# Used by both Streamlit Community Cloud and Hugging Face Spaces.
#
# For the full local training/notebook environment (albumentations extras,
# tensorboard, cleanlab, jupyter, etc.), see requirements-train.txt instead.
#
# CPU-only PyTorch wheels: the default PyPI torch/torchvision wheels bundle
# CUDA and are multi-GB, which is unnecessary (and can time out or exceed
# storage limits) on CPU-tier hosts like HF Spaces' free tier or Community
# Cloud. codes/utils.get_device() already falls back to CPU automatically
# when no MPS/CUDA device is available.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.12.0
torchvision==0.27.0

streamlit>=1.46.0
numpy==2.4.6
pillow==12.2.0
pandas==3.0.3
opencv-python-headless==4.13.0.92
matplotlib==3.10.9
seaborn>=0.13.0
scikit-learn==1.9.0
albumentations==2.0.8
joblib==1.5.3
plotly>=6.1.2
openpyxl>=3.1.5
requests>=2.32.3
